# --- Core ---
pydantic==2.11.7
pydantic-settings==2.7.1
python-dotenv==1.2.2  # >=1.2.2 fixes CVE-2026-28684 (symlink file-overwrite)
typer==0.15.1
click==8.1.8  # transitive: typer 0.15.1 breaks on click>=8.2 (make_metavar() signature change)
httpx==0.28.1

# --- Azure ---
azure-identity==1.19.0
azure-mgmt-resourcegraph==8.0.0
azure-mgmt-costmanagement==4.0.1
azure-monitor-query==1.4.0
azure-mgmt-advisor==9.0.0
azure-mgmt-compute==33.0.0
azure-mgmt-network==28.0.0
six==1.17.0  # transitive: azure-mgmt-resourcegraph 8.0.0 imports it but doesn't declare it

# --- AI ---
anthropic==0.69.0  # >=0.45 required for the `thinking` param (adaptive thinking); 0.42 rejects it
openai==1.59.6

# --- Storage ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.13
alembic==1.14.0

# --- API / scheduler ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
apscheduler==3.11.0
